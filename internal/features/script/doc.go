// Package script implements the rAthena script engine (WS-D): lexer,
// parser, stack-based bytecode VM, and hot-reload controller.
// Embedded in the zone service — not a separate binary.
package script
