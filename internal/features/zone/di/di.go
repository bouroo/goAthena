// Package di wires the zone feature into the DI container.
package di

import "github.com/samber/do/v2"

// Register wires the zone feature (map instances, AOI, tick loop).
// TODO(DEL-03): implement.
func Register(c do.Injector) error {
	return nil
}
